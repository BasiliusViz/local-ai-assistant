# MCP-сервер поиска по базе знаний (kb/)
mcp==2.0.0
qdrant-client==1.19.0
httpx==0.28.1
# выгрузка Confluence (confluence/sync.py) — запускается в этом же контейнере,
# чтобы на сервере не требовался Python
requests>=2.31
